{% set twigTemplate = 'searchform.html.twig' %}
{% set scope = 'searchform' %}

{% block searchform %}

    <form role="search" method="get" class="search-form form" action="{{ site.url }}/">
        <label>
            <span class="screen-reader-text">{{ __('Search for:', 'g5_iwi') }}</span>
            <input type="search" class="search-field" placeholder="{{ __( 'Search …', 'g5_iwi' ) }}" value="" name="s" title="{{ __('Search for:', 'g5_iwi') }}" />
        </label>
        <input type="submit" class="search-submit button button-small" value="Search" />
    </form>

{% endblock %}
